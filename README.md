repomix --remove-comments --compress --ignore "**/*_test.go,**/*.html,web/**/*"

# TON-618 v2

**TON-618 v2** é um motor de busca pessoal (PKM) que indexa arquivos Markdown, combina busca textual **FTS5**. Tudo com um frontend **HTMX + Tailwind CDN** — sem dependências npm e com inicialização em **< 100ms**.

HAAAA
package notes

import "ton618/core/web/layout"
import "ton618/core/internal/core/staticver"
import "ton618/core/internal/ui/icons"

templ Database(title string) {
    @layout.Layout(title, true, databaseContent())
}

templ databaseContent() {
<div>
    @templ.Raw("<script>window.TON_ICON_CONFIG = " + icons.ConfigJSON() + ";</script>")
    <!-- Header -->
    <div class="flex flex-col gap-4 mb-6">
        <div class="flex items-center justify-between">
            <h1 class="text-2xl font-black tracking-tight text-white flex items-center gap-2.5">
                @icons.Icon(icons.GetIcon("tabulator"), "w-6 h-6 " + icons.GetColor("tabulator")) Tabulator
            </h1>
            <div class="flex items-center gap-4">
                <!-- Contador: apenas QUANTAS notas estão sendo exibidas agora
                     (muda com a busca — ver setStats em database.js) -->
                <div class="text-sm font-semibold text-zinc-400 min-w-5 text-center" id="db-stats" title="Notas exibidas na tabela">0</div>
                
                <!-- Column Visibility Selector -->
                <div class="relative" id="column-selector-container">
                    <button id="column-selector-btn" class="flex items-center gap-1.5 px-3 py-1.5 text-xs font-semibold bg-zinc-900 border border-zinc-800 rounded-lg text-zinc-300 hover:text-white hover:border-zinc-700 transition-all focus:outline-hidden">
                        @icons.Icon("columns", "w-3.5 h-3.5")
                        Colunas
                    </button>
                    <!-- Dropdown Menu -->
                    <div id="column-selector-menu" class="absolute right-0 mt-2 w-56 origin-top-right bg-zinc-950/95 border border-zinc-800 rounded-xl shadow-2xl hidden z-50 p-3 max-h-80 overflow-y-auto">
                        <div class="text-[11px] font-bold text-zinc-500 uppercase tracking-wider mb-2 px-1">Exibir/Ocultar Colunas</div>
                        <div class="space-y-1" id="column-checkboxes">
                            <!-- Populated dynamically -->
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Search input and filters -->
        <div class="mt-2 flex items-center gap-2">
            <div class="relative flex-1 min-w-0">
                <input id="db-search" type="text" placeholder="Buscar em qualquer campo..." autocomplete="off"
                    class="w-full pl-4 pr-10 py-2.5 text-[13px] bg-zinc-900/60 border border-zinc-800 rounded-lg text-zinc-200 placeholder:text-zinc-500 focus:outline-hidden focus:border-sky-500 transition-all" />
                <button id="db-search-clear" type="button" class="absolute right-3 top-1/2 -translate-y-1/2 p-1 rounded-full text-zinc-500 hover:text-zinc-300 hover:bg-zinc-800/60 hidden transition-colors" title="Limpar busca">
                    @icons.Icon("x", "w-3.5 h-3.5")
                </button>
            </div>
            <!-- Alterna o modo de busca (ver applySearch/setupSearchModeButton).
                 "Pai + irmãos": resultado + caminho de pais + irmãs.
                 "Só resultados": apenas as notas que casaram. -->
            <button id="db-search-mode" type="button" aria-pressed="false"
                class="shrink-0 flex items-center gap-1.5 px-3 py-2.5 text-[12px] font-semibold bg-zinc-900/60 border border-zinc-800 rounded-lg text-zinc-300 hover:text-white hover:border-zinc-700 focus:outline-hidden focus:border-sky-500 transition-all"
                title="Busca: mostrando o resultado com o caminho de pais e as irmãs — clique para mostrar só as notas que casaram">
                <span id="db-search-mode-icon-contexto">@icons.Icon("git-fork", "w-3.5 h-3.5")</span>
                <span id="db-search-mode-icon-resultados" class="hidden">@icons.Icon("search", "w-3.5 h-3.5")</span>
                <span id="db-search-mode-label">Pai + irmãos</span>
            </button>
        </div>
    </div>

    <!-- Tabulator container -->
    <div id="notes-table" class="w-full bg-zinc-950/50 rounded-xl overflow-hidden border border-zinc-800"></div>
</div>

<!-- Tabulator Assets loaded dynamically below -->

<style>
    /* Tabulator Overrides to match the premium dark mode of TON-618 */
    #notes-table {
        width: 100vw;
        position: relative;
        left: 50%;
        right: 50%;
        margin-left: -50vw;
        margin-right: -50vw;
        border-radius: 0 !important;
    }
    .tabulator {
        background-color: transparent !important;
        border: none !important;
        font-family: var(--font-sans) !important;
        font-size: 13px;
    }
    .tabulator-header {
        background-color: #18181b !important; /* zinc-900 */
        border-bottom: 1px solid #27272a !important; /* zinc-800 */
        color: #a1a1aa !important; /* zinc-400 */
        font-weight: 700 !important;
    }
    .tabulator-col {
        background-color: #18181b !important;
        border-right: 1px solid #27272a !important;
    }
    .tabulator-row {
        background-color: #09090b !important; /* zinc-950 */
        border-bottom: 1px solid #27272a !important; /* zinc-800 */
        color: #d4d4d8 !important; /* zinc-300 */
        transition: background-color 0.15s;
    }
    .tabulator-row:hover {
        background-color: #18181b !important; /* zinc-900 */
    }
    .tabulator-row.tabulator-row-even {
        background-color: #09090b !important; /* zinc-950 */
    }
    .tabulator-cell {
        border-right: 1px solid #27272a !important;
        padding: 8px 12px !important;
    }
    .tabulator-cell input {
        background-color: #27272a !important;
        color: #fff !important;
        border: 1px solid #3f3f46 !important;
        border-radius: 4px;
        padding: 4px;
    }
    /* Editable cells indication */
    .tabulator-cell.tabulator-editable:hover {
        background-color: rgba(14, 165, 233, 0.1) !important; /* sky-500/10 */
        cursor: pointer;
    }
    /* Cell Formatters styling */
    .tag-pill {
        display: inline-block;
        background-color: rgba(255, 255, 255, 0.1);
        color: #e4e4e7;
        font-size: 10px;
        font-weight: 700;
        padding: 2px 6px;
        border-radius: 4px;
        margin-right: 4px;
        margin-bottom: 2px;
    }

    /* ── Hierarquia de notas (módulo Data Tree) ──
       Controle padrão do Tabulator, recolorido para o dark mode do TON-618.
       ⚠️ Não use dataTreeExpandElement/CollapseElement: o Tabulator substitui o
       controle inteiro por eles (some o wrapper e o tabIndex) — ver database.js. */
    #notes-table .tabulator-data-tree-control {
        background: #18181b !important; /* zinc-900 */
        border: 1px solid #3f3f46 !important; /* zinc-700 */
        border-radius: 5px !important;
        width: 15px !important;
        height: 15px !important;
        margin-right: 7px !important;
        justify-content: center;
    }
    #notes-table .tabulator-data-tree-control:hover {
        background: #27272a !important; /* zinc-800 */
        border-color: #0ea5e9 !important; /* sky-500 */
    }
    /* Barras do +/- do controle padrão (o tema claro as desenha em branco) */
    #notes-table .tabulator-data-tree-control .tabulator-data-tree-control-expand,
    #notes-table .tabulator-data-tree-control .tabulator-data-tree-control-collapse {
        background: #a1a1aa !important; /* zinc-400 */
    }
    #notes-table .tabulator-data-tree-control .tabulator-data-tree-control-expand:before,
    #notes-table .tabulator-data-tree-control .tabulator-data-tree-control-expand:after,
    #notes-table .tabulator-data-tree-control .tabulator-data-tree-control-collapse:before,
    #notes-table .tabulator-data-tree-control .tabulator-data-tree-control-collapse:after {
        background: #a1a1aa !important; /* zinc-400 */
        border-color: #a1a1aa !important;
    }
    #notes-table .tabulator-data-tree-control:hover .tabulator-data-tree-control-expand,
    #notes-table .tabulator-data-tree-control:hover .tabulator-data-tree-control-collapse,
    #notes-table .tabulator-data-tree-control:hover .tabulator-data-tree-control-expand:before,
    #notes-table .tabulator-data-tree-control:hover .tabulator-data-tree-control-expand:after,
    #notes-table .tabulator-data-tree-control:hover .tabulator-data-tree-control-collapse:before,
    #notes-table .tabulator-data-tree-control:hover .tabulator-data-tree-control-collapse:after {
        background: #38bdf8 !important; /* sky-400 */
        border-color: #38bdf8 !important;
    }

    /* Premium Dark Mode Footer & Pagination styling */
    .tabulator-footer {
        background-color: #09090b !important; /* zinc-950 */
        border-top: 1px solid #27272a !important; /* zinc-800 */
        color: #a1a1aa !important; /* zinc-400 */
        padding: 10px 16px !important;
    }
    .tabulator-footer .tabulator-paginator {
        color: #a1a1aa !important;
        display: flex;
        align-items: center;
        gap: 6px;
    }
    .tabulator-footer select {
        background-color: #18181b !important; /* zinc-900 */
        border: 1px solid #27272a !important; /* zinc-800 */
        color: #d4d4d8 !important; /* zinc-300 */
        border-radius: 6px;
        padding: 4px 8px !important;
        font-weight: 500;
        outline: none;
        cursor: pointer;
    }
    .tabulator-footer select:focus {
        border-color: #0ea5e9 !important; /* sky-500 */
    }
    .tabulator-footer .tabulator-page {
        background-color: #18181b !important; /* zinc-900 */
        border: 1px solid #27272a !important; /* zinc-800 */
        color: #a1a1aa !important; /* zinc-400 */
        border-radius: 6px;
        padding: 4px 10px !important;
        font-weight: 600;
        font-family: inherit;
        transition: all 0.15s ease;
    }
    .tabulator-footer .tabulator-page:hover:not(.disabled) {
        background-color: #27272a !important; /* zinc-800 */
        color: #ffffff !important;
        border-color: #3f3f46 !important;
    }
    .tabulator-footer .tabulator-page.active {
        background-color: rgba(14, 165, 233, 0.15) !important; /* sky-500/15 */
        border-color: rgba(14, 165, 233, 0.4) !important; /* sky-500/40 */
        color: #38bdf8 !important; /* sky-400 */
        font-weight: 700;
    }
    .tabulator-footer .tabulator-page.disabled {
        opacity: 0.3;
        background-color: transparent !important;
        border-color: transparent !important;
        cursor: not-allowed;
    }
</style>

<script src={ staticver.URL("/static/database.js") }></script>
}
